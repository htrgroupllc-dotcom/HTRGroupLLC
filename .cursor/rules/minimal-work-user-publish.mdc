---
description: Все проекты HTR — агент пушит сам; frontend через HTRGroupLLC-DEPLOY
alwaysApply: true
---

# Минимум работы пользователя (все проекты HTR)

- **Агент (frontend):** из `C:\Projects\HTRGroupLLC1` → `node scripts/cf-frontend-deploy.mjs --push --verify` (target = этот clean clone `HTRGroupLLC-DEPLOY`)
- **Агент (API):** commit + push `HTRGroupLLC1`
- **Пользователь:** только Replit → **Publish**, если менялся backend/API
- **Запрещено:** GitHub Desktop Commit/Push как обычный frontend workflow; ZIP как основной метод; dirty clone `C:\Projects\HTRGroupLLC`

| Цель | Repo / путь | Пользователь |
|------|-------------|--------------|
| API / голос | HTRGroupLLC1 | Replit **Publish** |
| Сайт Cloudflare | HTRGroupLLC-DEPLOY → origin `HTRGroupLLC` `main` | Ничего (ждать Cloudflare) |
